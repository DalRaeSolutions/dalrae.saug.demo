{
  "name": "cds",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^5",
    "@sap/cds-odata-v2-adapter-proxy": "^1.8.1",
    "express": "^4"
  },
  "devDependencies": {
    "@sap/ux-specification": "^1.90.15",
    "jest": "^27.3.1",
    "sqlite3": "^5",
    "supertest": "^6.1.6"
  },
  "scripts": {
    "start": "npx cds run",
    "watch": "npx cds watch",
    "test": "jest --testTimeout=10000 --detectOpenHandles --verbose",
    "test:watch": "jest --testTimeout=2000 --watchAll --verbose --silent --detectOpenHandles"
  },
  "sapux": [
    "app/cattle",
    "app/paddocks"
  ],
  "cds": {}
}
