{
  "name": "cds",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^4",
    "express": "^4"
  },
  "devDependencies": {
    "@sap/ux-specification": "^1.78.13",
    "jest": "^26.6.3",
    "sqlite3": "^5",
    "supertest": "^6.0.1"
  },
  "scripts": {
    "start": "npx cds run",
    "watch": "npx cds watch",
    "test": "jest --testTimeout=2000 --detectOpenHandles --verbose",
    "test:watch": "jest --testTimeout=2000 --watchAll --verbose --detectOpenHandles"
  },
  "sapux": [
    "app/cattle"
  ]
}
